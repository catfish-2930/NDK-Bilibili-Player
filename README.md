# NDK-Bilibili Player

Search Bilibili videos with WBI search and add video webpage URLs to the NDKaraoke queue. This plugin deliberately does not call Bilibili `playurl` or resolve DASH URLs. Playback is handled by the host system's embedded Bilibili mpv Lua hook.
